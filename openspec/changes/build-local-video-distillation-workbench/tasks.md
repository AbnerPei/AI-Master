---
创建日期: 2026-06-12T16:36:42+08:00
title: 本地视频蒸馏工作台任务清单
status: draft
product_name: 片语蒸馏台
---

## 1. 工作台范围与接口

- [x] 1.1 明确第一版只支持“单本地 `mp4` + 单输出目录 + 单任务闭环”
- [x] 1.2 定义页面表单字段、任务状态字段、日志事件字段和结果文件清单
- [x] 1.3 定义本地桥接层最小接口，至少覆盖启动任务、查询状态、读取 `log` 和打开输出目录

## 2. 页面交互

- [x] 2.1 设计本地 `HTML` 页面的基础布局，至少覆盖输入区、任务状态区、日志区和结果区
- [x] 2.2 支持选择本地 `mp4`
- [x] 2.3 支持选择输出目录
- [x] 2.4 支持启动任务，并在运行时禁用重复提交
- [x] 2.5 在页面上同时展示阶段状态和逐条原始 `log`

## 3. 本地任务桥接

- [x] 3.1 实现页面到本地任务脚本的桥接入口
- [x] 3.2 让桥接层能够持续返回阶段更新和日志追加
- [x] 3.3 为失败任务补充结构化错误返回，而不是只返回退出码

## 4. 视频提取与转录

- [x] 4.1 实现输入校验，包括视频存在性、扩展名和输出目录可写性检查
- [x] 4.2 实现音频提取步骤
- [x] 4.3 实现文案提取策略顺序：已有字幕优先、本地 `ASR` 其次、云端转录兜底
- [x] 4.4 为转录结果记录来源字段、可用性字段和失败原因

## 5. 文案整理与结果输出

- [x] 5.1 实现原始转录、清洗转录和 `AI` 蒸馏的分层输出
- [x] 5.2 实现标准结果目录结构和稳定文件命名
- [x] 5.3 让 `meta.json` 记录输入文件、输出目录、转录来源、任务耗时和失败信息
- [x] 5.4 让页面在任务完成后展示结果文件列表和核心摘要预览

## 6. 验证与后续衔接

- [x] 6.1 用一个真实本地 `mp4` 跑通完整闭环
- [x] 6.2 校验输出 `Markdown` 是否符合当前仓库的书写规范
- [x] 6.3 评估是否需要补一层“导入到知识库固定目录”的后续变更
